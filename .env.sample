# ============================================
# Environment Configuration (.env.sample)
# ============================================
# WARNING: This is a template file for environment variables.
# DO NOT EXPOSE YOUR ACTUAL .env FILE TO THE PUBLIC!
#
# Copy this file to .env and replace placeholder values with your actual credentials.
# ============================================

# ============================================
# Discord Bot Configuration
# ============================================
# Your Discord application client ID
# Get this from: https://discord.com/developers/applications > Your App > OAuth2 > Client ID
BOT_CLIENT_ID=1234567890

# The ID of the Discord server (guild) where the bot operates
# Right-click your server and select "Copy Server ID" (enable Developer Mode first)
DISCORD_GUILD_ID=1234567890

# ============================================
# Google Cloud / Secret Manager Configuration
# ============================================
# (Optional) Explicit Google Cloud project ID/number for Secret Manager.
# If unset, the SDK will try to detect the project ID automatically from the environment.
GOOGLE_CLOUD_PROJECT_ID=your_gc_project_id

# (Optional) Secret IDs in Secret Manager. These are NOT the secret values.
# If unset, the code will fall back to the default IDs shown here.
# The effective secret name will be:
#   projects/${GOOGLE_CLOUD_PROJECT_ID}/secrets/${SECRET_ID}/versions/latest
DISCORD_BOT_TOKEN_SECRET_ID=DISCORD_BOT_TOKEN
OPENAI_API_KEY_SECRET_ID=OPENAI_API_KEY


# ============================================
# Database Configuration
# ============================================
# Database file name (connection URL)
# The "file:" prefix is required for local SQLite databases.
DB_FILE_NAME=file:./aibot.db

# ============================================
# Usage/Billing Limits
# ============================================
# Daily token limit per user (total tokens: input + output)
# Window resets at JST 00:00.
TOKEN_DAILY_LIMIT=10000

# LLM model configuration per command
CHAT_LLM_MODEL=gpt-4.1-nano
TALK_LLM_MODEL=gpt-4.1-nano

# Directory for exported daily token usage SQLite files (optional)
# Defaults to ./storage when unset.
TOKEN_USAGE_DAILY_ARCHIVE_DIR=./storage

# ============================================
# Secret Keys Configuration (Legacy/Development)
# ============================================
# NOTE: These environment variables are legacy and primarily for development.
# In production, secrets are managed via Google Cloud Secret Manager (see above).
# If these env vars are set and non-empty, they take precedence for local development.
DISCORD_BOT_TOKEN=your_discord_bot_token
OPENAI_API_KEY=your_openai_api_key
